#!/bin/bash -x
cd $BLENDER_GIT/../blender_local
git pull --rebase
git submodule foreach git pull --rebase origin master
#cd $BLENDER_SVN_LOCAL
#git pull --rebase
#git submodule foreach git pull --rebase origin master

